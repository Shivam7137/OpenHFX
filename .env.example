# Provider intentionally left unset. No real model call occurs without an adapter.
LLM_PROVIDER=
LLM_MODEL=

# Explicit example suggestions for local hackathon demonstrations.
OPENHFX_ENGINE_MODE=demo
OPENHFX_DEMO_MODE=true
OPENHFX_DATA_DIR=.data

# Demo sign-in is local only. Real identity/hosting must be configured before public use.
